import Section from '../components/Section';

//components
import Layout from '../layout/layout';

const Home = () => {
  return (
    <Layout>
      <Section />
    </Layout>
  );
};

export default Home;
